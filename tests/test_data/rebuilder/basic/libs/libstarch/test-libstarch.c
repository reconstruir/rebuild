#include <libstarch/amylose.h>
#include <libstarch/amylopectin.h>
#include <stdio.h>
#include <stdlib.h>

int amylose_foo(int x)
{
  return 100;
}

int amylose_bar(int x)
{
  return 200;
}

int amylopectin_foo(int x)
{
  return 600;
}

int amylopectin_bar(int x)
{
  return 700;
}

int main()
{
  int x = amylose_foo(0) + amylose_bar(int x)
    printf("%s\n", environ[i]);
  }
  return 0;
}
