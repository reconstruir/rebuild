!rebuild.recipe!

package libjpeg-9a-1
  properties

  steps  
    step_setup
      tarball
        all: ${REBUILD_PACKAGE_NAME}-${REBUILD_PACKAGE_UPSTREAM_VERSION}.tar.gz
    step_autoconf_configure
    step_shell
      cmd: make V=1
    step_shell
      cmd: make install prefix=$REBUILD_STAGE_PREFIX_DIR V=1
    step_artifact_create
